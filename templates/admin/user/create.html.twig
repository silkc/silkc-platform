{% extends 'admin/layout.html.twig' %}

{% block title %}Administration{% endblock %}

{% block content %}
    {% autoescape false %}
        <div class="row">
            <div class="col col-12 col-lg-9">
                <h2 class="mb-4">
                    {{ 'user.create.page_title'|trans({}, 'admin') }}
                </h2>

                {{ include('admin/user/_form.html.twig') }}
            </div>
            <div class="col col-12 col-lg-3">
            </div>
        </div>
    {% endautoescape %}
{% endblock %}