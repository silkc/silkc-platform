<table id="datatable-work" class="table table-striped table-bordered" style="width:100%">
    <thead>
        <tr>
            <th>{{ 'work.label.name'|trans({}, 'trad') }}</th>
            <th>{{ 'work.label.registered_on'|trans({}, 'trad') }}</th>
            <th>{{ 'work.label.registered_by'|trans({}, 'trad') }}</th>
            <th>{{ 'work.label.status'|trans({}, 'trad') }}</th>
            <th>{{ 'work.label.action'|trans({}, 'trad') }}</th>
        </tr>
    </thead>
    <tbody>
        {% if to_validated_trainings is defined and to_validated_trainings is iterable and to_validated_trainings|length > 0 %}
            {% for training in to_validated_trainings %}
                <tr>
                    <td>{{ training.name }}</td>
                    <td>{{ training.createdAt|date('Y/m/d') }}</td>
                    <td>
                        {{ training.user.firstname ~ ' ' ~ training.user.lastname }}
                        <i class="fas fa-info-circle" data-toggle="tooltip" title="{{ training.user.email }}"></i>
                    </td>
                    <td>
                        {% if (training.isValidated == true) %}
                            <span class="text-success">{{ 'work.label.approved'|trans({}, 'trad') }}</span>
                        {% elseif (training.isRejected == true)%}
                            <span class="text-warning">{{ 'work.label.rejected'|trans({}, 'trad') }}</span>
                        {% else %}
                            {{ 'work.label.pending'|trans({}, 'trad') }}
                        {% endif %}
                    </td>
                    <td>
                        <a href="" class="btn btn-success mt-1 mb-1">{{ 'work.action.approve'|trans({}, 'trad') }}</a>
                        <a href="" class="btn btn-success mt-1 mb-1">{{ 'work.action.reject'|trans({}, 'trad') }}</a>
                        <a href="#" class="btn btn-primary mt-1 mb-1 see-detail" data-name="{{ training.name }}" data-description="{{ training.description }}">{{ 'work.action.see_details'|trans({}, 'trad') }}</a>
                        <a href="" class="btn btn-danger mt-1 mb-1">{{ 'work.action.delete'|trans({}, 'trad') }}</a>
                    </td>
                </tr>
            {% endfor %}
        {% else %}
            <tr><td colspan="3">{{ 'no_item'|trans({}, 'trad') }}</td></tr>
        {% endif %}
    </tbody>
</table>
